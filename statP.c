#include "types.h"
#include "stat.h"
#include "user.h"
#include "fcntl.h"

int main(int argc, char *argv[])
{
  //char *pointer;
  
  //int c,d,count, direcciones;
  //count = 0;

  //int direccion;
  statP();
  //pointer = (char*)malloc(32+1);

  /*
  direcciones = 

  for (c = 31; c >= 0; c--)
  {
    d = *ptabledir >> c;
    if (d & 1)
      *(pointer + count) = 1 + '0';
    else
      *(pointer + count) = 0 + '0';
    count++;
  }
  *(pointer + count) = '\0';
*/
  
  exit();
}
